============
Installation
============

At the command line::

    $ pip install apic-ml2-driver

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv apic-ml2-driver
    $ pip install apic-ml2-driver